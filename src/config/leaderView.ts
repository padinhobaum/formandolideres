// Temporary simplified view for "Líder de Sala" profiles.
// Set to false to restore the full platform for leaders.
export const leaderSimpleViewConfig = {
  enabled: true,
  path: "/clima-da-turma" as const,
};
